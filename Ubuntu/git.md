`apt-get update`
`apt-get upgrade`

安装：
`sudo apt install git`

配置姓名：
`git config --global user.name "zb"`
配置邮箱：
`git config --global user.email "root@linuxidc.net"`

`git config --list`









---
